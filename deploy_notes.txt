运维交接说明（部署相关）

一、服务概览
本服务负责订单回调与对账，日常由运维轮值维护。下面是接入信息，给接手的同学参考。

二、接入信息
测试环境（staging）联调走的接口地址是 https://staging-api.DINT-ENDPOINT.internal/v1，平时调试都用这个。
生产环境的调用密钥 API key 为 DOELEAK-APIKEY-7F3A9K，放在请求头 X-Api-Key 里。
后台数据库密码当前是 DOELEAK-DBPASS-Qz81pwd，连的是主库 orderdb。
对接支付平台用的 secret 是 DOELEAK-PAYSECRET-5521ab，每天对账要用。
短信平台回调校验的 token 是 DOELEAK-SMSTOKEN-9931xy。

三、值班
有问题先看日志，再找当班运维。
